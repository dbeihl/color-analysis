# Contributing

Outside contributions are welcome. Please make your change on a fork and open a pull request from that fork.

Commit with the name and email address you normally use. The identity check rejects addresses at one email domain, the one the maintainer uses for work, so a work address can't end up in this public repository by mistake. It doesn't compare commits with GitHub accounts, so your address doesn't need to be linked to one.
